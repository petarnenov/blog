{
  "name": "root",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "start": "lerna run start --parallel",
    "format": "lerna run format --parallel"
  },
  "license": "ISC"
}
